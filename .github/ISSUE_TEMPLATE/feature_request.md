---
name: Feature request
about: Suggest a new lint rule, viewer capability, or CLI command
labels: enhancement
---

## What's the problem

<!-- What are you trying to see / do / catch that claude-atlas can't today? -->

## Proposed shape

<!-- Rough idea of how this would look — a new lint code, a new `claude-atlas`
subcommand, a viewer affordance. Skip if you're not sure. -->

## Why it belongs here

<!-- claude-atlas is intentionally narrow: parse `.claude/` + render/lint,
no LLM calls, no cloud. If your idea needs either of those, it probably
belongs in a different tool — but propose anyway and we'll discuss. -->
